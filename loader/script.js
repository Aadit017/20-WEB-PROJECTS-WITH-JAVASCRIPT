const loaderContainer = document.querySelector('.loader-container');
window.addEventListener('load',()=>{
    loaderContainer.classList.add('hide-loader');
    document.body.classList.add('show-oveflow');
});
